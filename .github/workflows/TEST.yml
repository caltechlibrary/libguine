name: TEST
on:
  push:
    # paths:
    #   - '*.s?html?'
    #   - '**.scss'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Diff
        run: git diff-tree --no-commit-id --name-only -r ${{ github.sha }}
